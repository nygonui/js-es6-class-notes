// Operados de atribuição
var x = 95;
var y = 37;
x += y;
x -= y;
x *= y;
x /= y;
x %= y;
x **= y;

// Operador Ternário
// Modo abreviado de fazer if e else
var idade = 19;
var podeBeber = (idade >= 18) ? 'Pode beber' : 'Não pode beber';
console.log(podeBeber);
// condição ? true : false
// Geralmente utilizado quando precisamos atribuir um valor a uma variável que depende de alguma regra

